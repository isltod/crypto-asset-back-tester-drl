# AI 에이전트 개발 규칙 (Node.js & Python .venv 프로젝트)

## 1. 기본 대화 규칙
- 개발자와의 모든 대화 및 설명은 **한국어**로 진행한다.
- 명확한 근거(로그, 코드 결과) 없이 추측으로 버그의 원인을 진단하지 않는다.

## 2. 개발 환경 및 실행 규칙
- **Python 환경 지침**:
  - 반드시 `.venv` 가상환경 내부의 Python 및 패키지를 사용한다.
  - 명령어 실행 시 `.venv/Scripts/python` (Windows) 또는 `.venv/bin/python` (Linux/macOS) 경로를 사용한다.
  - 패키지 설치 시에도 가상환경 내의 pip (`.venv/Scripts/pip install`)를 사용한다.
  - `.venv/` 폴더 내부의 파일을 직접 수정하거나 변경하지 않는다.

- **Node.js 환경 지침**:
  - 로컬 Node.js 런타임을 통해 실행하며, 패키지는 `npm`으로 관리한다.
  - `.gitignore`에 포함된 `node_modules/` 내부 파일을 직접 수정하지 않는다.

## 3. 코드 작성 및 품질 규칙
- **안전성 및 예외 처리**:
  - 비밀키, API Key 등 보안 정보는 절대 코드에 하드코딩하지 않고 `.env` 파일로 관리한다.
  - 에러를 단순히 가리거나 무시하는 무의미한 `try-catch` / `try-except` 구문을 작성하지 않는다.
  - 코드 변경 후 반드시 관련 테스트나 실행 명령어를 통해 정상 동작을 검증한다.

- **컨벤션**:
  - JavaScript: ES6+ 현대적 문법 및 `async/await` 비동기 처리를 우선 사용한다.
  - Python: PEP 8 스타일 가이드를 준수하며 필요시 타입 힌트(Type Hinting)를 작성한다.

## 4. 모듈 간 통신 및 아키텍처
- Node.js(백엔드/런타임)와 Python(데이터/AI 처리) 간의 데이터 교환 규격(JSON 구조 등)이 변경될 경우 관련 코드를 함께 업데이트한다.

## 5. 모듈화 및 코드 구조화 규칙 (Monolithic 코드 방지)
- **단일 책임 원칙 (Single Responsibility)**:
  - 하나의 파일/모듈은 한 가지 명확한 역할만 담당하도록 작성한다.
  - 한 파일의 코드가 비대해지면(약 200~300줄 초과 시) 적극적으로 유틸리티나 하위 모듈로 분리(Refactoring)한다.

- **디렉터리/역할 분리**:
  - **Node.js**: 라우터/컨트롤러(`routes/`, `controllers/`), 비즈니스 로직(`services/`), 공통 유틸(`utils/`), 설정(`config/`)을 구별하여 배치한다.
  - **Python**: 데이터 전처리(`data/`), 모델 및 알고리즘(`models/`), 유틸리티(`utils/`) 등으로 분리하여 작성한다.

- **신규 기능 추가 시 지침**:
  - 새로운 독립적 기능이나 헬퍼 함수를 추가할 때는 기존 단일 파일에 코드 폭탄처럼 덧붙이지 말고, 별도 파일/모듈로 작성한 뒤 `import` / `require`하여 사용한다.

